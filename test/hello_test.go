package main

import (
	// 	"assignment/cmd/main"
	"fmt"
	"testing"
)

func TestHello(t *testing.T) {
	// got := main.Hello()

	fmt.Println("Hello")

	// if got != want {
	// 	t.Errorf("got %q want %q", got, want)
	// }
}
